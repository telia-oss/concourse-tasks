platform: linux

image_resource:
  type: docker-image
  source:
    repository: wata727/tflint
    tag: "0.5.1"

inputs:
- name: terraform
- name: common-tasks

run:
  path: sh
  args:
  - -ec
  - |
    cd $PWD/terraform/
    tflint
